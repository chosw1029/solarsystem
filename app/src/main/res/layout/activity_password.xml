<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_password"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    tools:context="nextus.solarsystem.view.PasswordActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberPassword"
        android:ems="4"
        android:id="@+id/editPassword"
        android:layout_marginEnd="16dp"
        app:layout_constraintRight_toRightOf="@+id/activity_password"
        android:layout_marginStart="16dp"
        app:layout_constraintLeft_toLeftOf="@+id/activity_password"
        app:layout_constraintBottom_toBottomOf="@+id/activity_password"
        android:textColor="#ffffff"
        android:layout_marginBottom="304dp"
        android:digits="0123456789" />

    <TextView
        android:text="This application can only be accessed for a permitted group.\n\n Please, Enter Password.  "
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:textColor="#ffffff"
        app:layout_constraintBottom_toTopOf="@+id/editPassword"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="@+id/activity_password"
        android:layout_marginStart="16dp"
        app:layout_constraintLeft_toLeftOf="@+id/activity_password"
        android:layout_marginEnd="16dp"
        app:layout_constraintRight_toRightOf="@+id/activity_password"
        android:gravity="center_horizontal"
        android:textSize="24sp" />

    <Button
        android:text="Enter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/button"
        android:layout_marginTop="80dp"
        app:layout_constraintTop_toBottomOf="@+id/editPassword"
        app:layout_constraintBottom_toBottomOf="@+id/activity_password"
        android:layout_marginBottom="16dp"
        app:layout_constraintVertical_bias="0.0"
        android:layout_marginEnd="16dp"
        app:layout_constraintRight_toRightOf="@+id/activity_password"
        android:layout_marginStart="16dp"
        app:layout_constraintLeft_toLeftOf="@+id/activity_password" />
</android.support.constraint.ConstraintLayout>
